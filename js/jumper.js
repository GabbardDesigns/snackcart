
document.addEventListener('click',(event) => {
    var splits = (event.target.id).split('-');
    var mykey = parseInt(splits[1]);
    console.log(mykey);


  });
  
document.addEventListener('click',(event) => {
    var splits = (event.target.id).split('-');
    var mykey = parseInt(splits[1]);
    console.log(mykey);

    
  });
  